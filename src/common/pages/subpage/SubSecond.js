import React, { Component } from 'react';

export default class SubSecond extends Component {
    static defaultProps={
        Title:"SubSecond"
    }
    render(){
        return <div> Sub Second</div>
    }
}